# Git metadata (this folder is mirrored to GitHub separately).
.git
# Unity Version Control ignore rules for PaperTrails.
# Regenerable / machine-local output stays out of the repo.
Library
Temp
Obj
Logs
Builds
UserSettings
.utmp
/Tests/bin
/Tests/obj
# Session/build logs (regenerable).
*.log
# Third-party reference drops (incl. commercial APK) - stays local only.
/Reference
# Installer debris, not project content.
/Tools
*.csproj.user
.vs
